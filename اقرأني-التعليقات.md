# نظام التعليقات — خطوات التركيب

> **الحزمة كود فقط.** ما فيها ولا ملف محتوى ولا إعدادات. إعداداتك وتعديلاتك على اللوحة ما رح تتأثر.

## 1) ارفع الملفات على GitHub

اسحب مجلدات الحزمة على الـ repo وارفعها فوق القديمة:

```
worker/index.ts
worker/schema.sql
worker/tsconfig.json
wrangler.jsonc
tsconfig.json
src/components/Comments.astro
src/content.config.ts
src/lib/site.ts
src/pages/lessons/[...slug].astro
src/pages/articles/[...slug].astro
src/pages/courses/[...slug].astro
public/admin/config.yml
```

**لا ترفع لسا** — أول اعمل قاعدة البيانات (خطوة 2) لأنه الرفع بيشغّل نشر تلقائي، وبدون القاعدة النشر بيفشل.

---

## 2) اعمل قاعدة بيانات D1

من لوحة Cloudflare:

1. **Storage & Databases → D1 SQL Database → Create**
2. الاسم بالضبط: `testing-arabic-comments`
3. بعد ما تنعمل، انسخ الـ **Database ID** (سطر طويل فيه شرطات)

---

## 3) حط الـ Database ID بملف `wrangler.jsonc`

بملف `wrangler.jsonc` اللي بالحزمة، بدّل السطر:

```jsonc
"database_id": "00000000-0000-0000-0000-000000000000"
```

بالـ ID اللي نسخته. عدّله **قبل** ما ترفع الملف على GitHub.

---

## 4) اعمل جداول القاعدة

بصفحة قاعدة البيانات بـ Cloudflare، افتح تبويب **Console**، وانسخ محتوى ملف `worker/schema.sql` كامل والصقه، ودوس **Execute**.

بتطلعلك رسالة نجاح، وبيصير عندك جدول اسمه `comments`.

---

## 5) حط كلمة سر لصفحة المراجعة

**Workers & Pages → testing-arabic → Settings → Variables and Secrets → Add**

- Type: **Secret**
- Name: `ADMIN_PASSWORD`
- Value: كلمة سر قوية من عندك (احفظها عندك)

دوس **Save**.

---

## 6) ارفع الملفات (خطوة 1) وانتظر النشر

بعد الرفع، Cloudflare بينشر لحاله. تابع من **Workers & Pages → testing-arabic → Deployments**.

---

## 7) شغّل التعليقات من اللوحة

ادخل على `/admin` (لوحة المحتوى) → **إعدادات الموقع → عامة**، وبتلاقي 4 حقول جديدة:

| الحقل | الشرح |
|---|---|
| **تفعيل التعليقات** | شغّلها لتظهر خانة التعليقات |
| **عنوان قسم التعليقات** | افتراضياً «النقاش» |
| **ملاحظة تحت العنوان** | «اكتب سؤالك أو ملاحظتك…» |
| **نص داخل خانة الكتابة** | placeholder |

احفظ وانشر.

---

## صفحة المراجعة

`https://موقعك/admin/comments`

- بتطلب كلمة السر اللي حطيتها بخطوة 5
- 3 تبويبات: **بانتظار المراجعة** / **منشورة** / **سبام**
- لكل تعليق: **نشر** · **إرجاع للانتظار** · **سبام** · **حذف** · **اكتب رد**
- ردّك بيظهر تحت التعليق بعلامة **«صاحب المنصة»**، وبينشر التعليق الأصلي تلقائياً
- الجلسة بتضل مفتوحة أسبوعين، وفي زر **خروج**

الصفحة `noindex` — ما بتطلع بجوجل.

---

## كيف بيشتغل

- التعليق بينحفظ **بانتظار المراجعة**، وما بيظهر لحدا غيرك لحد ما توافق عليه
- الاسم مستعار وبس — ما في تسجيل دخول ولا إيميل ولا كوكيز للزوار
- ما بينحفظ IP أبداً: بينحفظ بصمة مشفّرة منه، وبس لمنع السبام
- 3 فلاتر تلقائية قبل ما يوصلك: فخ مخفي للبوتات، رفض الإرسال بأقل من 3 ثواني، ورفض أكثر من رابطين
- 12 تعليق بالساعة كحد أقصى من نفس المصدر
- خانة التعليقات بتظهر على **الدروس والدورات والمقالات**. الأخبار بدون تعليقات (زي ما اتفقنا)
- قائمة التعليقات ما بتظهر إلا لما يصير في تعليق منشور واحد على الأقل — يعني ما في «قسم فاضي» بالصفحات الجديدة

## التكلفة

صفر. D1 المجاني بيعطي 5 مليون قراءة و100 ألف كتابة باليوم — أضعاف أضعاف اللي بتحتاجه.
