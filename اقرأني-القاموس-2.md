# قاموس المصطلحات — الدفعة الثانية (15 مصطلح)

> محتوى، مش كود — بس **كل الملفات جديدة**، فحصت أسماءها مقابل الـ32 مصطلح الموجودين عندك حالياً وما في تقاطع. ولا إشي بينمسح.

## الرفع

```
src/content/glossary/    ← 15 ملف جديد
```

قاموسك بيصير **47 مصطلح** بدل 32.

---

## شو فيها

**إدارة الاختبار (5)**
`test-data` بيانات الاختبار · `test-suite` مجموعة اختبارات · `traceability-matrix` مصفوفة التتبّع RTM · `risk-based-testing` الاختبار المبني على المخاطر · `test-coverage` تغطية الاختبار

**أنواع الاختبار (3)**
`functional-testing` الوظيفي · `non-functional-testing` غير الوظيفي · `confirmation-testing` اختبار التأكيد

**تقنيات التصميم (3)**
`decision-table` جدول القرار · `state-transition` انتقال الحالات · `error-guessing` تخمين الأخطاء

**منهجيات (2)**
`tdd` · `bdd`

**أتمتة (2)**
`page-object-model` نمط Page Object · `emulator-vs-real-device` المحاكي مقابل الجهاز الحقيقي

---

## الفحص

- **البناء نجح** — 90 صفحة، ولا خطأ بالـ frontmatter
- **الـ15 صفحة بترد 200** بعد البناء
- **20 رابط داخلي، كلهم فحصتهم مقابل موقعك الحقيقي** — بما فيهم `/resources/rtm-template/` اللي تأكدت إنه موجود قبل ما أربط عليه

## الربط مع الدفعة الأولى

مصطلحات هاي الدفعة بتربط على مصطلحات الدفعة الأولى (`shift-left`، `unit-testing`، `test-pyramid`، `defect-lifecycle`، `static-testing`، `black-box`…). يعني القاموس صار شبكة مترابطة مش قائمة منفصلة — وهاد اللي بتحبّه جوجل وبيخلي الزائر يتنقّل بدل ما يطلع.

---

## اللي ضلّ للدفعة الثالثة

**إدارة:** Test Basis · Test Oracle · Test Charter · Test Estimation · Bug Density · Impact Analysis · Maintenance Testing

**تقنيات:** Statement Coverage · Branch Coverage · Checklist-based Testing

**مراجعات:** Walkthrough · Inspection · Technical Review · Work Product

**أتمتة وأدوات:** Locator · Test ID · Explicit Wait · Device Lab · Appium · Maestro · CI/CD

**منهجيات:** ATDD · V-model · DevOps

تقريباً 22 مصطلح. قلّي وقت ما تكون جاهز.
