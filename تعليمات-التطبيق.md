# تحديث: صورة غلاف الفيديو من لوحة التحكّم

## المشكلة

مشغّل Bunny كان بياخد صورة الغلاف من Bunny نفسه — لقطة عشوائية من الفيديو.
وصورة الغلاف اللي ترفعها من اللوحة كانت تنستخدم بس بكرت الدرس وبالمشاركة
على السوشال، وما إلها أي تأثير على المشغّل.

## الحل

صورة الغلاف اللي ترفعها من اللوحة صارت تنعرض فوق المشغّل مع زر تشغيل
برتقالي. ضغطة واحدة من الزائر بتمرّ للمشغّل الحقيقي وبيشتغل الفيديو،
وبنفس اللحظة الطبقة بتختفي.

الشغل كله صار بمكان واحد (Base.astro)، فما في أي تعديل على صفحات الدروس
ولا على أي محتوى.

إذا الدرس بلا صورة غلاف، أو الصورة ما زبطت بالتحميل، ما بتنحطّ أي طبقة
والمشغّل بيضلّ زي ما هو بالضبط.

---

## التطبيق — ارفع الملفين زي ما هم، بلا أي تعديل يدوي

```
src/components/VideoFacade.astro     (نسخة محدّثة — استبدل القديم)
src/layouts/Base.astro               (سطرين إضافة فقط)
```

من GitHub: افتح المجلد ← Add file ← Upload files ← اسحب الملف ← Commit.

**التغيير الوحيد بـ Base.astro** هو سطرين:

```astro
import VideoFacade from '../components/VideoFacade.astro';
...
<VideoFacade poster={props.type === 'video.other' ? props.image : undefined} />
```

باقي الملف حرف بحرف زي الأصل (تأكّدت بمطابقة SHA-256).

---

## ملف للحذف

```
تعليمات-التطبيق.md        ← من جذر الريبو (انرفع بالغلط مع الرفعة السابقة)
```

---

## بعد النشر

افتح `/lessons/fundamentals-of-testing/` — لازم تشوف صورتك المصمّمة
(ISTQB CTFL 4.0 / أساسيات الاختبار / CH 1) مع زر تشغيل برتقالي بالوسط،
وبضغطة واحدة يبدأ الفيديو.

## ملاحظتان

**الفيديو الترحيبي بالرئيسية** لسه بياخد صورته من Bunny — لأنه صفحة
الرئيسية ما بتمرّر صورة للـ Base. لو بتحب نعمله نفس الشي، قلّي وبجهّزه.

**تصميم الغلاف:** زر التشغيل بينزل بوسط الصورة بالضبط، فخلّي وسط
التصميم فاضي نسبياً بالأغلفة الجديدة.

**Bunny:** ما ضل داعي ترفع thumbnails هناك إطلاقاً.
