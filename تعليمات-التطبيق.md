# تحديث: صورة غلاف الفيديو من لوحة التحكّم

## شو بيعمل هذا التحديث

قبل: مشغّل Bunny كان بياخد صورة الغلاف من Bunny نفسه — لقطة عشوائية من الفيديو،
وصورة الغلاف اللي ترفعها من اللوحة كانت تظهر بس على كرت الدرس وبالمشاركة على السوشال.

بعد: صورة الغلاف اللي ترفعها من اللوحة تصير هي صورة المشغّل نفسه. وكبونص،
مشغّل Bunny ما بينحمّل إلا لما يضغط الزائر زر التشغيل — الصفحة تفتح أسرع
وتتحسّن نتيجة LCP بجوجل.

إذا الدرس ما إله صورة غلاف، المشغّل يشتغل عادي زي قبل — ولا شي يتغيّر.

---

## 1) ملف جديد — ارفعه كما هو

```
src/components/VideoFacade.astro
```

موجود جوّا الزيب. ارفعه على نفس المسار (Add file ← Upload files وأنت جوّا
مجلد `src/components`).

---

## 2) تعديل: src/layouts/Base.astro  (سطرين)

**أ.** دوّر على السطر:

```
import CursorFx from '../components/CursorFx.astro';
```

وضيف تحته سطر جديد:

```
import VideoFacade from '../components/VideoFacade.astro';
```

**ب.** دوّر على السطر:

```
<CursorFx />
```

وضيف تحته:

```
<VideoFacade />
```

---

## 3) تعديل: src/pages/lessons/[...slug].astro  (تعديلين)

**أ.** دوّر على:

```
    <div class="player">
```

وبدّله بـ:

```
    <div class="player" data-poster={data.thumbnail}>
```

**ب.** جوّا نفس البلوك، دوّر على:

```
          src={embed}
```

وبدّله بـ:

```
          data-src={embed}
```

---

## 4) تعديل: src/pages/index.astro  (تعديلين — للفيديو الترحيبي بالرئيسية)

**أ.** انتبه: في الملف أكثر من `<div class="thumb">`. المطلوب هو **اللي بعد**
السطر `<div class="feat card welcome">` بالضبط. بدّله من:

```
          <div class="thumb">
```

إلى:

```
          <div class="thumb" data-poster={home.welcomeThumbnail}>
```

**ب.** دوّر على:

```
                src={welcomeEmbed}
```

وبدّله بـ:

```
                data-src={welcomeEmbed}
```

---

## 5) بعد النشر — تأكيد

1. افتح `/lessons/fundamentals-of-testing/` — لازم تشوف صورتك المصمّمة
   (ISTQB CTFL 4.0 / أساسيات الاختبار / CH 1) مع زر تشغيل برتقالي.
2. اضغط الزر — لازم الفيديو يبدأ فوراً.
3. افتح الرئيسية — نفس الشي للفيديو الترحيبي.

## ملاحظة على Bunny

بعد هذا التحديث ما ضل داعي ترفع thumbnails على Bunny إطلاقاً. لو حبيت
ترفعها هناك برضو، لازم تكون **JPG**، أبعاد **1280×720**، وأقل من **1MB**،
وبعد ما يخلص ترميز الفيديو 100% — وإلا Bunny يرفضها بصمت أو الترميز
يدهسها.

## ملاحظة على المسافات البادئة

المسافات البادئة بالأسطر فوق تقريبية للتوضيح. المهم النص نفسه — خلّي
المسافة البادئة زي ما هي بالسطر الأصلي عندك.
