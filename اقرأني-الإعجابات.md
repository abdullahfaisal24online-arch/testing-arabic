# زر الإعجاب — خطوات التركيب

> **كود فقط.** ما فيها ولا ملف محتوى ولا إعدادات. كل تعديلاتك على اللوحة بأمان.
> **وما فيها `wrangler.jsonc`** عن قصد — لأنه فيه رقم قاعدة بياناتك، وما بدي أستبدله. بتعدّله بإيدك بخطوة ٢.

امشِ بالترتيب.

---

## ١) اعمل جدول الإعجابات

لوحة Cloudflare → **Storage & Databases → D1** → افتح `testing-arabic-comments` → تبويب **Console**.

انسخ محتوى `worker/schema-likes.sql` كامل والصقه، ودوس **Execute**.

جدول التعليقات ما بيتأثر إطلاقاً — هذا جدول جديد منفصل.

---

## ٢) عدّل سطر واحد بملف `wrangler.jsonc`

على GitHub، افتح `wrangler.jsonc` ودوس قلم التعديل. دوّر على السطر اللي بيبدأ بـ `"run_worker_first"`.

**قبل:**

```jsonc
"run_worker_first": ["/api/comments", "/api/comments/*", "/admin/comments", "/admin/comments/*"]
```

**بعد:**

```jsonc
"run_worker_first": ["/api/comments", "/api/comments/*", "/api/likes", "/api/likes/*", "/admin/comments", "/admin/comments/*"]
```

يعني بس ضفنا `"/api/likes", "/api/likes/*",` بالنص. احفظ (Commit).

> بدون هالسطر Cloudflare بتعترض الطلب وترجّع صفحة 404 بدل ما توصل للخادم — نفس المشكلة اللي صارت معنا بالتعليقات.

---

## ٣) ارفع ملفات الحزمة

اسحب المجلدات على الـ repo وارفعها فوق القديمة:

```
worker/index.ts                     ← بيستبدل القديم
worker/schema-likes.sql             ← جديد
src/components/LikeButton.astro     ← جديد
src/content.config.ts               ← بيستبدل القديم
src/lib/site.ts                     ← بيستبدل القديم
src/pages/lessons/[...slug].astro   ← بيستبدل القديم
src/pages/articles/[...slug].astro  ← بيستبدل القديم
public/admin/config.yml             ← بيستبدل القديم
```

Cloudflare بتنشر لحالها. تابع من **Workers & Pages → testing-arabic → Deployments**.

---

## ٤) شغّله من اللوحة

ادخل `/admin` → **إعدادات الموقع → عامة**، وبتلاقي ٣ حقول جديدة:

| الحقل | الشرح |
|---|---|
| **تفعيل زر الإعجاب** | شغّلها ليظهر الزر |
| **كلمة زر الإعجاب** | افتراضياً «أعجبني» |
| **أظهر العدد بعد كم إعجاب** | افتراضياً 3 — حط 0 لو بدك الرقم يبين دايماً |

احفظ وانشر.

---

## ٥) تأكد إنه شغال

افتح أي درس:

1. لازم تشوف الزر تحت عنوان الدرس مباشرة، قلب فاضي وكلمة «أعجبني» بدون رقم
2. دوس — القلب بينملي برتقالي وبتصير حركة صغيرة
3. حدّث الصفحة — لازم يضل ملوّن (المتصفح متذكّرك)
4. دوس مرة تانية — بينفكّ

إذا الزر ما ظهر أصلاً، غالباً خطوة ٤ ما انحفظت. إذا ظهر بس ما بيستجيب، غالباً خطوة ٢.

---

## كيف بيشتغل

- **بدون تسجيل دخول** — ولا إيميل ولا حساب
- المتصفح بيتذكر إنك دُست (يعني على جهاز تاني بيبان فاضي — طبيعي بدون حسابات)
- ما بينحفظ IP: بس بصمة مشفّرة منه لمنع التكرار
- **بيظهر على الدروس والمقالات** — مش على الأخبار ولا صفحات الدورات
- العدّاد بينجلب بطلب صغير بعد ما تفتح الصفحة، فما بيأثر على سرعة الموقع
- حدود الحماية: 8 إعجابات كحد أقصى لنفس الصفحة من نفس المصدر، و40 حركة بالساعة. مرفوعة عن قصد لأنه كثير ناس بتطلع من نفس الـ IP على شبكات الموبايل
- إذا ما في نت، الزر بيرجع لحالته الأصلية بدل ما يضل عالق

## التكلفة

صفر. الإعجاب صف واحد بقاعدة D1، والمجاني بيعطيك 100 ألف كتابة و5 مليون قراءة **باليوم**.

## لاحقاً لو بدك

أقدر أضيف على صفحة `/admin/comments` قائمة «أكثر المحتوى إعجاباً» — بتعطيك إشارة مباشرة عن شو بيشتغل مع جمهورك. قلّي وقت ما تحب.
