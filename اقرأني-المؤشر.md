# مؤشر المثلّث 🔻 + موجة النقر

ملف واحد بس: `src/components/CursorFx.astro` — فيه الـ CSS والـ JS مع بعض، وصورة
المثلّث مدمجة جوّه كـ data URI (مش ملف خارجي، فما في طلب شبكة إضافي ولا وميض
أول تحميل).

## التركيب — سطرين

افتح الـ layout الأساسي (الملف اللي جوّه `src/layouts/` وفيه `<html>` و `<slot />`
— غالباً `BaseLayout.astro` أو `Layout.astro`):

**١. بأعلى الملف، جوّه الـ frontmatter (بين الـ `---`):**

```astro
import CursorFx from '../components/CursorFx.astro';
```

**٢. جوّه `<body>`، قبل `</body>` بالضبط:**

```astro
<CursorFx />
```

وبس. بما إنه بالـ layout، بيشتغل على كل صفحات الموقع تلقائياً.

> لوحة `/admin` وصفحة `/admin/comments` **مش** رح يتأثروا — هدول تطبيقات
> مستقلة ما بتمر على الـ layout. وهاد مقصود: المؤشر للزوار، مش لشغلك.

## شو بيصير بالضبط

**المؤشر:** مثلّث أحمر مقلوب (32×32، لون `#dd2e44` مسطّح مع حدّ أبيض رفيع)
بيظهر فوق الروابط، الأزرار، الكروت، `summary`، `select`، والـ checkbox/radio —
وبيورَّث تلقائياً لكل إشي جوّاهم (النص، الأيقونات، الصور).

**بيضل طبيعي:** النص العادي (مؤشر عادي)، حقول الكتابة (مؤشر كتابة)، والعناصر
المعطّلة (`not-allowed`).

**عند النقر:** مثلّث رفيع بنفس الأحمر بيطلع من نقطة النقر، يتوسع من 35% لـ 150%
ويخفت لصفر بـ 340 مللي ثانية. بيتحرك بـ `transform` و`opacity` بس — يعني على
كرت الشاشة، ما بيسبب إعادة تخطيط ولا تقطيع.

## الحدود المحسوبة (مش صدفة)

| الحالة | السلوك |
|---|---|
| موبايل وتابلت (لمس) | كل إشي مطفي — المؤشر والموجة. `@media (hover: hover) and (pointer: fine)` |
| مفعّل «تقليل الحركة» بنظامه | المؤشر بيضل، **الموجة بتنطفي** |
| كليك يمين أو زر أوسط | ما في موجة |
| عنصر معطّل | ما في موجة، والمؤشر `not-allowed` |
| نقر متسارع | حد أقصى 4 موجات بنفس اللحظة، وكلهن بينمسحوا بعد ما يخلصوا |
| متصفح قديم بلا Web Animations | الموجة بتتخطى بهدوء، والمؤشر بيضل شغّال |
| صورة المؤشر ما انحملت | بيرجع لـ `pointer` العادي (fallback مكتوب بالـ CSS) |

## تعديلات محتملة

**عنصر قابل للنقر ما أخذ المؤشر** (مثلاً `div` مع `onclick`): ضيف عليه كلاس
`tri-cursor` وبياخذه.

**بدك الموجة على كل نقرة بالموقع، مش بس القابل للنقر:** بملف الـ component،
جوّه معالج `pointerdown`، بدّل هالثلاث سطور:

```js
const hit = e.target instanceof Element ? e.target.closest(CLICKABLE) : null;
if (!hit) return;
if (hit.matches(':disabled, [aria-disabled="true"]')) return;
```

بهالسطرين:

```js
const hit = e.target instanceof Element ? e.target.closest(CLICKABLE) : null;
if (hit && hit.matches(':disabled, [aria-disabled="true"]')) return;
```

(بس رأيي تخليها زي ما هي — الموجة على كل نقرة بتصير ضجيج، وبتفقد معناها:
هلق هي إشارة «الشي اللي نقرته استجاب».)

**بدك تغيّر لون أو حجم المثلّث:** لون الموجة بالـ CSS جوّه `.tri-ripple polygon`
(`stroke`). أما لون المؤشر نفسه فمخبّي جوّه صورة الـ PNG المدمجة — قلّي أي لون
وأي حجم بدك وبولّدلك صورة جديدة.

**السرعة:** رقم `duration: 340` جوّه الـ component.

## للتجربة قبل التركيب

`cursor-demo.html` — افتحه بأي متصفح على جهازك. فيه أزرار وروابط وكرت وحقل
كتابة ونص عادي، بنفس ألوان موقعك، عشان تحسّ بالتأثير قبل ما ترفع إشي.
